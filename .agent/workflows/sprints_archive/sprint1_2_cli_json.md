# Micro-Sprint 1.2: JSON CLI Support

**Objective**: Update `supervisor_cli.py` to support a `--json` flag that outputs structured logs.

**Status**: PLANNING

## Backlog
- [ ] Update `new_agent/automation/supervisor_cli.py` to handle `--json` argument
- [ ] Verify `supervisor_cli.py` works with `python automation/supervisor_cli.py start --json`
